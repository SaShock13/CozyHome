using System.Collections.Generic;
using UnityEngine;

public class GalleryView : MonoBehaviour
{    

    //[SerializeField] private Transform contentRoot;
    //[SerializeField] private GalleryItemView itemPrefab;

    //private readonly List<GalleryItemView> spawnedItems = new();

    //public void ShowItems(List<ImageItemData> items)
    //{
    //    Clear();

    //    foreach (var item in items)
    //    {
    //        var view = Instantiate(itemPrefab, contentRoot);
    //        view.SetData(item);
    //        spawnedItems.Add(view);
    //    }
    //}

    //private void Clear()
    //{
    //    foreach (var item in spawnedItems)
    //        Destroy(item.gameObject);

    //    spawnedItems.Clear();
    //}
}
